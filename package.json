{
  "name": "missionary-media-website",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "check:deploy": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
